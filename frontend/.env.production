# Production Environment Variables for Frontend
# API calls go through Vercel's rewrite proxy (/api/* -> Railway backend)
# This avoids cross-origin issues on mobile Safari (ITP "Load failed" error)
VITE_API_URL=/api
